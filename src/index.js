import React from 'react';
import ReactDOM from 'react-dom';
import GLIApp from './GLIApp';
import './css/main.css';

ReactDOM.render(
  <React.StrictMode>
    <GLIApp />
  </React.StrictMode>,
  document.getElementById('root')
);
